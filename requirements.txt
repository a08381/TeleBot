# Telegram Bot 框架
python-telegram-bot>=21.0,<23.0

# HTTP
httpx>=0.27,<1.0

# 浏览器指纹（TLS/JA3 + HTTP/2）：过 Cloudflare 靠它，装上才能用
# browser.impersonate；没装会自动退回 httpx（功能不受影响，只是没有指纹）
curl_cffi>=0.9,<1.0

# 可选：需要 HTTP/2 时
# httpx[http2]>=0.27,<1.0
